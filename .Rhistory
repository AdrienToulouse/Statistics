Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
set.seed(0)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
set.seed(0)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
set.seed(1)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
set.seed(2)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
set.seed(3)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
set.seed(4)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
set.seed(5)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
set.seed(6)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
set.seed(7)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
set.seed(8)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
set.seed(9)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
set.seed(10)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
set.seed(11)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
set.seed(11)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(Nattemps, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
set.seed(11)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(Nattempts, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
set.seed(11)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
qqnorm(gauss_sample)
qqline(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - 3) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - gauss_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - gauss_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - gauss_sample))/ sqrt(mean(poisson_sample))
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - gauss_sample)/ sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - gauss_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - gauss_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - gauss_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - gauss_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - gauss_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - gauss_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
qqnorm(gauss_sample)
knitr::opts_chunk$set(echo = TRUE)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - gauss_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
qqnorm(gauss_sample)
qqline(gauss_sample)
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
hist(poi)
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
hist(poisson_sample)
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
hist(gauss_sample)
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
hist(gauss_sample)
qqnorm(gauss_sample)
qqline(gauss_sample)
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
hist(gauss_sample)
sqrt(mean(poisson_sample)
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
hist(gauss_sample)
sqrt(mean(poisson_sample)
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
hist(gauss_sample)
sqrt(mean(poisson_sample))
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
hist(gauss_sample)
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
hist(gauss_sample)
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
hist(gauss_sample)
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
hist(gauss_sample)
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
hist(gauss_sample)
gauss_sample[1]
qqnorm()
qqnorm(gauss_sample)
qqline(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(sample)
qqnorm(sample)
qqline(sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(sample)
Nattempts = 100000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(sample)
knitr::opts_chunk$set(echo = TRUE)
data(USJudgeRatings)
head(USJudgeRatings)
str(USJudgeRatings)
sum(is.na(USJudgeRatings))
Outvals = boxplot(USJudgeRatings)
max(USJudgeRatings$CONT)
rownames(USJudgeRatings)[which.max(USJudgeRatings$CONT)]
min(USJudgeRatings$RTEN)
rownames(USJudgeRatings)[which.min(USJudgeRatings$RTEN)]
max(USJudgeRatings$RTEN)
rownames(USJudgeRatings)[which.max(USJudgeRatings$RTEN)]
summary(USJudgeRatings)
par(mfrow=c(2,2))
hist(USJudgeRatings$CONT, main="CONT")
hist(USJudgeRatings$PREP, main="PREP" )
hist(USJudgeRatings$WRIT, main="WRIT")
hist(USJudgeRatings$RTEN, main="RTEN")
round(sqrt(diag(var(USJudgeRatings))),2)
round(cor(USJudgeRatings),2)
library(corrplot)
corrplot(cor(USJudgeRatings))
pairs(USJudgeRatings)
par(mfrow=c(1,2))
hist(USJudgeRatings$CONT, probability= TRUE, main="Histogram of CONT", xlab="CONT")
d = density(USJudgeRatings$CONT, kernel = 'c', bw = 0.3)
lines(d, col="red")
hist(USJudgeRatings$RTEN, probability= TRUE, main="Histogram of RTEN" , xlab="RTEN")
d = density(USJudgeRatings$RTEN, kernel = 'o', bw = 0.3)
lines(d, col="red")
par(mfrow=c(1,2))
plot(ecdf(USJudgeRatings$CONT), verticals = TRUE, do.points = FALSE, main = "ECDF CONT")
plot(ecdf(USJudgeRatings$RTEN), verticals = TRUE, do.points = FALSE, main = "ECDF RTEN")
qqnorm(USJudgeRatings$RTEN)
qqline(USJudgeRatings$RTEN)
library(tinytex)
