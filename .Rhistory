hist(gauss_sample)
Nattempts = 1000
nsample = 100
est_theta <- list(moy = c(),  median=c(), minmax = c())
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
est_theta <- list(moy = c(),  median=c(), minmax = c())
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
est_theta <- list(moy = c(),  median=c(), minmax = c())
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
set.seed(0)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
set.seed(0)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
set.seed(1)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
set.seed(2)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
set.seed(3)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
set.seed(4)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
set.seed(5)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
set.seed(6)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
set.seed(7)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
set.seed(8)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
set.seed(9)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
set.seed(10)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
set.seed(11)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
set.seed(11)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(Nattemps, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
set.seed(11)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(Nattempts, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
set.seed(11)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
qqnorm(gauss_sample)
qqline(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - 3) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - gauss_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - gauss_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - gauss_sample))/ sqrt(mean(poisson_sample))
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - gauss_sample)/ sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - gauss_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - gauss_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - gauss_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - gauss_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - gauss_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - gauss_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
qqnorm(gauss_sample)
knitr::opts_chunk$set(echo = TRUE)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - gauss_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(gauss_sample)
qqnorm(gauss_sample)
qqline(gauss_sample)
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
hist(poi)
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
hist(poisson_sample)
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
hist(gauss_sample)
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
hist(gauss_sample)
qqnorm(gauss_sample)
qqline(gauss_sample)
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
hist(gauss_sample)
sqrt(mean(poisson_sample)
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
hist(gauss_sample)
sqrt(mean(poisson_sample)
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
hist(gauss_sample)
sqrt(mean(poisson_sample))
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
hist(gauss_sample)
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
hist(gauss_sample)
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
hist(gauss_sample)
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
hist(gauss_sample)
poisson_sample = rpois(nsample, lambda = 3)
gauss_sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
hist(gauss_sample)
gauss_sample[1]
qqnorm()
qqnorm(gauss_sample)
qqline(gauss_sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(sample)
qqnorm(sample)
qqline(sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(sample)
Nattempts = 100000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(sample)
Nattempts = 1000
nsample = 100
for (i in 1:Nattempts)  # can be written without the for loop (nicer) !
{
poisson_sample = rpois(nsample, lambda = 3)
sample = sqrt(nsample) * (mean(poisson_sample) - poisson_sample) / sqrt(mean(poisson_sample))
}
hist(sample)
